# qwen2.5:3b-instruct, not the plan's recommended 7b -- see docs/decisions.md
# Phase 0 entry (slow network during the build). Swap in a larger model here
# with no code change if it proves too erratic (§1.2).
ENERGYPLUS_DIR=/Applications/EnergyPlus-26-1-0
OLLAMA_MODEL=qwen2.5:3b-instruct
OLLAMA_HOST=http://localhost:11434
